function particle = createParticle(speed,angle,xPos,yPos,r,m)

particle.speed = speed;

particle.angle = angle;

particle.xPos = xPos;

particle.yPos = yPos;

particle.radius = r;

particle.mass = m;
